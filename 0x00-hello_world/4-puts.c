#include <stdio.h>

/**
 *  * main - Entry point of the program
 *   *
 *    * Return: Always 0 (success)
 *     */
int main(void)
{
	    const char *message = "Hello, World!";
	        puts(message); 

		    return (0);
}
